# Avti

Avti is a branded desktop fork of the DeepSeek Harness desktop shell.

The fork keeps upstream-internal identifiers intact where practical, while replacing user-visible product identity, installer naming, desktop application id, visual tokens, and update behavior.

See `AVTI_BRANDING.md` for the maintenance strategy.
